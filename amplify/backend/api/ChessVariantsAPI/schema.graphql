type User @model {
  id: ID!
  username: String!
  points: Int!
  skillLevel: String!
  rank: Int!
}

type Game @model {
  id: ID!
  creator: User! @connection
  creatorSocketId: String!
  creatorOrientation: String!
  variant: String
}

