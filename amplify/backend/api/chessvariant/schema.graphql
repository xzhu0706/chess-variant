type User @model{
  id: ID!
  username: String!
  skillLevel: String!
  rank: Int!
  points: Int!

}
type Game @model {
  id: ID!
  creator: User @connection
  creatorConnectionID: String!
  minutesPerSide: Int!
  variant: String!
}

type ConnectionID @models {
  id: ID!
  connectionID: String!
}